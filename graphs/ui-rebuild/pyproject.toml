[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "burncloud-ui-rebuild"
version = "1.0.0"
description = "BurnCloud Graph Engineering Harness v1 for Buyer, Supplier and Admin console delivery."
requires-python = ">=3.11"
dependencies = [
  "langgraph>=1.1,<2.0",
  "langchain>=1.3.14,<2.0",
  "langchain-openai>=1.0,<2.0",
  "python-dotenv>=1.0,<2.0",
  "pydantic>=2.7,<3.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[project.scripts]
burncloud-ui-rebuild = "burncloud_ui_rebuild.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/burncloud_ui_rebuild"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
